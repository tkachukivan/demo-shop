export enum Role {
  Admin,
  User,
}

export enum Gender {
  Unisex = 'Unisex',
  Man = 'Man',
  Woman = 'Woman'
}

export enum ProductFilters {
  Page = 'page',
  Limit = 'limit',
  Search = 'search',
  Available = 'availableOnly',
  Cost = 'cost'
}
